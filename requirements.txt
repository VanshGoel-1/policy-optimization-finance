# Data science and utilities
pandas==2.1.2
numpy==1.26.2
scipy==1.11.3
scikit-learn==1.2.2
matplotlib==3.8.1
jupyterlab==4.2.1
pyarrow==12.0.1

# Deep learning (Use CONDA for installation, not PIP)
# We list them here for documentation, but recommend installing via
# conda install pytorch torchvision cpuonly -c pytorch
torch
torchvision

# Offline RL (Use CONDA for installation)
# We list it here, but recommend installing via
# conda install d3rlpy -c conda-forge
d3rlpy

# Kaggle API for dataset download
kaggle==1.5.16

# Misc
tqdm==4.66.1
joblib==1.3.2
